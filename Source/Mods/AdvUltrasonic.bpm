Function ReadPort1(out number distance)
  distance = Sensor1.Raw1() / 10
EndFunction

Function ReadPort2(out number distance)
  distance = Sensor2.Raw1() / 10
EndFunction

Function ReadPort3(out number distance)
  distance = Sensor3.Raw1() / 10
EndFunction

Function ReadPort4(out number distance)
  distance = Sensor4.Raw1() / 10
EndFunction
